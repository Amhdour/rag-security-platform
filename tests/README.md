# Tests Directory

This top-level `tests/` directory is reserved for repository-level evidence-driven tests.

Current executable test suites live in:
- `integration-adapter/tests/`
- `myStarterKit-maindashb-main/tests/`

Use this folder when adding root-level contract/integration tests that span repository boundaries.
